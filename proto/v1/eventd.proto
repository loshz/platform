syntax = "proto3";

package service.v1;

option go_package = "github.com/loshz/platform/pkg/pb/v1;pbv1";

service Eventd {
  // Event takes host level events.
  rpc Event(EventRequest) returns (EventResponse) {}
}

message EventRequest { string hostname = 1; }

message EventResponse { string uuid = 1; }
