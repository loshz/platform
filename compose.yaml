services:
  prometheus:
    image: prom/prometheus:v2.48.0
    command:
      - '--config.file=/etc/prometheus/prometheus.yaml'
    ports:
      - 9090:9090
    volumes:
      - ./config/prometheus/prometheus.yaml:/etc/prometheus/prometheus.yaml

  grafana:
    image: grafana/grafana:10.2.2
    depends_on:
      - prometheus
    ports:
      - 3000:3000
    volumes:
      - ./config/grafana/grafana.ini:/etc/grafana/grafana.ini
      - ./config/grafana/dashboard-platform-overview.json:/var/lib/grafana/dashboards/platform-overview.json
      - ./config/grafana/dashboard.yaml:/etc/grafana/provisioning/dashboards/dashboard.yaml
      - ./config/grafana/datasource.yaml:/etc/grafana/provisioning/datasources/datasource.yaml

  discoveryd:
    build: .
    command: discoveryd
    environment:
      PLAT_SERVICE_DISCOVERY_ENABLED: false
      PLAT_HTTP_PORT: 8001
      PLAT_GRPC_TLS_CA: "/home/platform/certs/ca.crt.pem"
      PLAT_GRPC_SERVER_PORT: 8000
      PLAT_GRPC_SERVER_CERT: "/home/platform/certs/server.crt.pem"
      PLAT_GRPC_SERVER_KEY: "/home/platform/certs/server.key.pem"

  trafficd:
    build: .
    command: trafficd
    environment:
      PLAT_SERVICE_DISCOVERY_ENABLED: false
      PLAT_HTTP_PORT: 8002
      PLAT_GRPC_TLS_CA: "/home/platform/certs/ca.crt.pem"
      PLAT_GRPC_CLIENT_CERT: "/home/platform/certs/client.crt.pem"
      PLAT_GRPC_CLIENT_KEY: "/home/platform/certs/client.key.pem"

  eventd:
    build: .
    command: eventd
    environment:
      PLAT_HTTP_PORT: 8003
      PLAT_GRPC_TLS_CA: "/home/platform/certs/ca.crt.pem"
      PLAT_GRPC_SERVER_PORT: 8004
      PLAT_GRPC_SERVER_CERT: "/home/platform/certs/server.crt.pem"
      PLAT_GRPC_SERVER_KEY: "/home/platform/certs/server.key.pem"
      PLAT_GRPC_CLIENT_CERT: "/home/platform/certs/client.crt.pem"
      PLAT_GRPC_CLIENT_KEY: "/home/platform/certs/client.key.pem"
